# منصة أبشر - نظام التعرف على الوجوه

نظام متكامل للإبلاغ عن الأشخاص المفقودين مع التعرف التلقائي على الوجوه.

## 🎯 الميزات

✅ **واجهة ويب احترافية** - تصميم منصة أبشر الرسمي
✅ **إضافة بلاغات** - نموذج كامل لإضافة بيانات الشخص المفقود
✅ **رفع الصور** - رفع صورة الشخص المفقود
✅ **التعرف التلقائي** - نظام Python للتعرف على الوجوه بالكاميرا
✅ **دعم اللغة العربية** - كامل في الواجهة والتعرف

---

## 📦 التثبيت

### 1. تثبيت المكتبات المطلوبة:

```bash
pip install -r requirements.txt
```

**ملاحظة:** إذا واجهت مشاكل مع opencv-python، استخدم:

```bash
pip uninstall opencv-python opencv-contrib-python -y
pip install opencv-contrib-python
```

---

## 🚀 طريقة الاستخدام

### الخطوة 1: تشغيل الخادم

```bash
python bridge.py
```

ستظهر رسالة:
```
🚀 تشغيل خادم منصة أبشر...
📍 افتح المتصفح على: http://localhost:5000
```

### الخطوة 2: فتح المتصفح

افتح المتصفح على: **http://localhost:5000**

### الخطوة 3: إضافة بلاغ

1. انقر على "بلاغ عن شخص مفقود"
2. املأ بيانات المبلغ
3. انقر "إرسال البلاغ"
4. املأ بيانات الشخص المفقود
5. **ارفع صورة واضحة للشخص**
6. انقر "إرسال البلاغ"

### الخطوة 4: التعرف التلقائي

- ستفتح نافذة الكاميرا **تلقائياً**
- انظر للكاميرا مباشرة
- سيتم التعرف عليك وعرض اسمك فوق وجهك!

---

## 📁 هيكل الملفات

```
منجز/
├── index.html              # الصفحة الرئيسية
├── missing-person.html     # نموذج بيانات المبلغ
├── thank-you.html          # نموذج بيانات الشخص المفقود + رفع الصورة
├── received-report.html    # صفحة الشكر
├── bridge.py               # خادم Flask للربط بين HTML و Python
├── camera_recognition.py   # نظام التعرف على الوجوه
├── face_recognizer.py      # محرك التعرف على الوجوه
├── database.py             # قاعدة البيانات
├── data/
│   └── photos/            # مجلد حفظ الصور
└── images/                # شعارات أبشر

```

---

## 🔧 حل المشاكل

### المشكلة: "فشل في فتح الكاميرا"

**الحل:**
1. تأكد من أن الكاميرا متصلة
2. أغلق أي تطبيق يستخدم الكاميرا (Zoom, Teams, إلخ)
3. أعد تشغيل البرنامج

### المشكلة: "خطأ في الاتصال بالخادم"

**الحل:**
1. تأكد من تشغيل `bridge.py` أولاً
2. تأكد من أن المنفذ 5000 غير مستخدم
3. جرب إعادة تشغيل الخادم

### المشكلة: "لم يتم العثور على وجه في الصورة"

**الحل:**
1. استخدم صورة واضحة بإضاءة جيدة
2. تأكد من أن الوجه أمامي (Frontal)
3. استخدم صورة تحتوي على وجه واحد فقط

### المشكلة: "الأسماء العربية لا تظهر"

**الحل:**
1. تأكد من تثبيت المكتبات:
   ```bash
   pip install arabic-reshaper python-bidi
   ```
2. أعد تشغيل البرنامج

---

## 💡 نصائح للحصول على أفضل نتائج

1. **الإضاءة:** استخدم إضاءة جيدة عند التقاط الصور
2. **الوضوح:** استخدم صور عالية الجودة
3. **الوجه:** تأكد من أن الوجه واضح وأمامي
4. **الكاميرا:** انظر مباشرة للكاميرا عند التعرف

---

## 📝 ملاحظات

- النظام يعمل على **Windows, macOS, Linux**
- يحتاج **كاميرا ويب** (Webcam)
- يدعم **اللغة العربية** بالكامل
- الصور تُحفظ في `data/photos/`
- قاعدة البيانات في `data/face_recognition.db`

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. تأكد من تثبيت جميع المكتبات
2. تأكد من تشغيل `bridge.py` أولاً
3. راجع رسائل الخطأ في PowerShell/Terminal

---

**تم التطوير بواسطة:** فريق منصة أبشر 🇸🇦
